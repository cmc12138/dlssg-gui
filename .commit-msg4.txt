一个游戏只允许一条记录：添加时查重 + 历史重复条目一键合并

用户实测发现的坑：同一个游戏在列表里能有两条（扫描后再手动加一次就会出现），
两条各带一份配置，注入状态还会互相干扰。

- 新增 src/main/games.ts：路径归一化比较（严格=目录完全相同；宽松=还认父子目录）、
  重复分组、合并（保留有注入记录的那条，否则保留最早创建的；历史、能力标记、注入记录一并合并，
  备份目录 rename 到保留条目下并改写记录里的路径，所以合并后仍然能还原）
- gamesAdd 先查重：同一个渲染目录/安装目录已存在就返回原来那条（created=false），不再新建
- 新增 IPC games:duplicates / games:merge-duplicates；游戏页发现有重复时给出提示与「合并重复条目」按钮
- 测试 19 → 21 项：目录判定 + 合并全流程（含备份迁移后还原成功）
